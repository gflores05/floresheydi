import React from 'react';

import Logo from './Logo';
import Menu from './Menu';

const header = () => (
    <header id="header">
        <div className="">
            <Logo></Logo>
            <Menu></Menu>
        </div>
    </header>
)
export default header;